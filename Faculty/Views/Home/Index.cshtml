@using Faculty.WEB.ViewModel;
@model FullListViewModel
@{
    ViewData["Title"] = "Faculty";
}

<h1>@ViewData["Title"]</h1>

<div class="form-group">
    <form method="post" class="form-inline" role="form">
        <label for="courseId" class="col-md-2 control-label">Course:</label>
        <select name="courseId" id="courseId" class="col-md-3 form-control">
            <option value="0" selected>All</option>
            @foreach (var course in Model.Courses)
            {
                <option value="@course.CourseId">@course.Name</option>
            }
        </select>
        <input type="submit" asp-controller="Courses" asp-action="ChooseCourse" value="Groups">
    </form>
</div>
<div class="form-group">
    <form method="post" class="form-inline" >
        <label for="groupId" class="col-md-2 control-label">Group:</label>
        <select name="groupId" id="groupId" class="col-md-3 form-control">
            <option value="0" selected>All</option>
            @foreach (var group in Model.Groups)
            {
                <option value="@group.GroupId">@group.Name</option>
            }
        </select>
        <input type="submit" name="submit" asp-controller="Groups" asp-action="ChooseGroup" value="Students">
        <input type="submit" name="edit" asp-controller="Groups" asp-action="EditGroup" asp-route-id="groupId" value="Edit">
        <input type="submit" name="delete" asp-controller="Groups" asp-action="DeleteGroup" asp-route-id="groupId" value="Delete">
    </form>
</div>
<div class="form-group">
    <form method="post" class="form-inline">
        <label for="studentId" class="col-md-2 control-label">Student:</label>
        <select name="studentId" id="studentId" class="col-md-3 form-control">
            <option value="0" selected>All</option>
            @foreach (var student in Model.Students)
            {
                <option value="@student.StudentId">@(student.FirstName + " " + student.LastName)</option>
            }
        </select>
        <input type="submit" name="edit" asp-controller="Students" asp-action="EditStudent" asp-route-id="studentId" value="Edit">
    </form>
</div>

<div class="row">
    <div class="column col-md-3">
        <h3>Courses</h3>
        @foreach (var course in Model.Courses)
        {
            @course.Name<br>
        }
    </div>
    <div class="column col-md-3">
        <h3>Groups</h3>
        @foreach (var group in Model.Groups)
        {
            @group.Name<br>
        }
    </div>
    <div class="column col-md-6">
        <h3>Students</h3>
        @foreach (var student in Model.Students)
        {
            @(student.FirstName + " " + student.LastName)<br>
        }
    </div>
</div>